# --- must not be published: the original assignment prompt (contains no code) ---
os_project_prompt.txt

# --- build artifacts ---
/target/
**/target/

# --- extracted ELF of the embedded kernel (rebuilt every `cargo build`) ---
/elfs/

# --- transient debug captures (QEMU -d/-debugcon, runner scratch) ---
qemu-int.log
qemu-run.out
dbgout.log
boot.out
r.out
r.err
r.code

# --- local tooling (per-task constraint: downloads live inside the repo dir) ---
/tools/

# --- fuzz & coverage artifacts ---
/fuzz/corpus/
/fuzz/artifacts/
/coverage/
*.profraw
*.profdata
llvm-cov-target/

# --- editor / OS cruft ---
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
*.swp
*~

# --- local overrides ---
.env
.env.local